[project]
name = "proxmox-skill"
version = "0.1.0"
description = "Public Codex skill and uv-powered CLI for safe Proxmox VE VM and LXC operations."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  { name = "Sunwood AI Labs" }
]

[tool.uv]
package = false
